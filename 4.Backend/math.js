/*const sum = (a, b) => a + b;
const diff = (a, b) => a - b;
const g = 9.8;

let obj = {
  sum: sum,
  diff: diff,
  g: g,
};

module.exports = obj;
 */

export const sum = (a, b) => a + b;
export const diff = (a, b) => a - b;
export const g = 9.8;
